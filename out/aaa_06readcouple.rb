require "scanf.rb"

for i in (1 ..  3) do
    (a, b) = STDIN.readline.split(" ").map{ |x| x.to_i(10) }
    printf "a = %d b = %d\n", a, b
    end
    l = STDIN.readline.split(" ").map{ |x| x.to_i(10) }
    for j in (0 ..  9) do
        printf "%d\n", l[j]
        end
        