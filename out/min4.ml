let min2 a b =
  min a b

let () =
begin
  let h = 1 in
  let i = 2 in
  let j = 3 in
  let k = 4 in
  let l = min2 h i in
  Printf.printf "%d " (min2 (min2 l j) k);
  let o = 1 in
  let p = 2 in
  let _q = 4 in
  let _r = 3 in
  let _s = min2 o p in
  Printf.printf "%d " (min2 (min2 l j) k);
  let v = 1 in
  let w = 3 in
  let _x = 2 in
  let _y = 4 in
  let _z = min2 v w in
  Printf.printf "%d " (min2 (min2 l j) k);
  let bc = 1 in
  let bd = 3 in
  let _be = 4 in
  let _bf = 2 in
  let _bg = min2 bc bd in
  Printf.printf "%d " (min2 (min2 l j) k);
  let bj = 1 in
  let bk = 4 in
  let _bl = 2 in
  let _bm = 3 in
  let _bn = min2 bj bk in
  Printf.printf "%d " (min2 (min2 l j) k);
  let bq = 1 in
  let br = 4 in
  let _bs = 3 in
  let _bt = 2 in
  let _bu = min2 bq br in
  Printf.printf "%d\n" (min2 (min2 l j) k);
  let bx = 2 in
  let by = 1 in
  let _bz = 3 in
  let _ca = 4 in
  let _cb = min2 bx by in
  Printf.printf "%d " (min2 (min2 l j) k);
  let ce = 2 in
  let cf = 1 in
  let _cg = 4 in
  let _ch = 3 in
  let _ci = min2 ce cf in
  Printf.printf "%d " (min2 (min2 l j) k);
  let cl = 2 in
  let cm = 3 in
  let _cn = 1 in
  let _co = 4 in
  let _cp = min2 cl cm in
  Printf.printf "%d " (min2 (min2 l j) k);
  let cs = 2 in
  let ct = 3 in
  let _cu = 4 in
  let _cv = 1 in
  let _cw = min2 cs ct in
  Printf.printf "%d " (min2 (min2 l j) k);
  let cz = 2 in
  let da = 4 in
  let _db = 1 in
  let _dc = 3 in
  let _dd = min2 cz da in
  Printf.printf "%d " (min2 (min2 l j) k);
  let dg = 2 in
  let dh = 4 in
  let _di = 3 in
  let _dj = 1 in
  let _dk = min2 dg dh in
  Printf.printf "%d\n" (min2 (min2 l j) k);
  let dn = 3 in
  let dp = 1 in
  let _dq = 2 in
  let _dr = 4 in
  let _ds = min2 dn dp in
  Printf.printf "%d " (min2 (min2 l j) k);
  let dv = 3 in
  let dw = 1 in
  let _dx = 4 in
  let _dy = 2 in
  let _dz = min2 dv dw in
  Printf.printf "%d " (min2 (min2 l j) k);
  let ec = 3 in
  let ed = 2 in
  let _ee = 1 in
  let _ef = 4 in
  let _eg = min2 ec ed in
  Printf.printf "%d " (min2 (min2 l j) k);
  let ej = 3 in
  let ek = 2 in
  let _el = 4 in
  let _em = 1 in
  let _en = min2 ej ek in
  Printf.printf "%d " (min2 (min2 l j) k);
  let eq = 3 in
  let er = 4 in
  let _es = 1 in
  let _et = 2 in
  let _eu = min2 eq er in
  Printf.printf "%d " (min2 (min2 l j) k);
  let ex = 3 in
  let ey = 4 in
  let _ez = 2 in
  let _fa = 1 in
  let _fb = min2 ex ey in
  Printf.printf "%d\n" (min2 (min2 l j) k);
  let fe = 4 in
  let ff = 1 in
  let _fg = 2 in
  let _fh = 3 in
  let _fi = min2 fe ff in
  Printf.printf "%d " (min2 (min2 l j) k);
  let fl = 4 in
  let fm = 1 in
  let _fn = 3 in
  let _fo = 2 in
  let _fp = min2 fl fm in
  Printf.printf "%d " (min2 (min2 l j) k);
  let fs = 4 in
  let ft = 2 in
  let _fu = 1 in
  let _fv = 3 in
  let _fw = min2 fs ft in
  Printf.printf "%d " (min2 (min2 l j) k);
  let fz = 4 in
  let ga = 2 in
  let _gb = 3 in
  let _gc = 1 in
  let _gd = min2 fz ga in
  Printf.printf "%d " (min2 (min2 l j) k);
  let gg = 4 in
  let gh = 3 in
  let _gi = 1 in
  let _gj = 2 in
  let _gk = min2 gg gh in
  Printf.printf "%d " (min2 (min2 l j) k);
  let gn = 4 in
  let gp = 3 in
  let _gq = 2 in
  let _gr = 1 in
  let _gs = min2 gn gp in
  Printf.printf "%d\n" (min2 (min2 l j) k)
end
 