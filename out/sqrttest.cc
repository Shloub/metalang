#include <iostream>
#include <vector>
#include<cmath>

int main(){
  int b = 4;
  std::cout << (int)sqrt(b) << " ";
  int e = 16;
  std::cout << (int)sqrt(e) << " ";
  int g = 20;
  std::cout << (int)sqrt(g) << " ";
  int i = 1000;
  std::cout << (int)sqrt(i) << " ";
  int k = 500;
  std::cout << (int)sqrt(k) << " ";
  int m = 10;
  std::cout << (int)sqrt(m) << " ";
  return 0;
}

