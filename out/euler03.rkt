#lang racket
(require racket/block)

(define main
  (let ([maximum 1])
  (let ([b0 2])
  (let ([a 408464633])
  (let ([sqrtia (integer-sqrt a)])
  (letrec ([d (lambda (a b0 sqrtia) 
                (if (not (eq? a 1))
                (let ([b b0])
                (let ([found #f])
                (letrec ([e (lambda (a b b0 found sqrtia) 
                              (if (<= b sqrtia)
                              ((lambda (internal_env) (apply (lambda (a b b0 found sqrtia) 
                                                                    (let ([b (+ b 1)])
                                                                    (e a b b0 found sqrtia))) internal_env)) 
                              (if (eq? (remainder a b) 0)
                              (let ([a (quotient a b)])
                              (let ([b0 b])
                              (let ([b a])
                              (let ([sqrtia (integer-sqrt a)])
                              (let ([found #t])
                              (list a b b0 found sqrtia))))))
                              (list a b b0 found sqrtia)))
                              (if (not found)
                              (block
                                (map display (list a "\n"))
                                (let ([a 1])
                                (d a b0 sqrtia))
                                )
                              (d a b0 sqrtia))))])
                (e a b b0 found sqrtia))))
                '()))])
  (d a b0 sqrtia))))))
)

