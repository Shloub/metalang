#lang racket
(require racket/block)
(define array_init_withenv (lambda (len f env)
  (build-vector len (lambda (i)
    (let ([o ((f i) env)])
      (block
        (set! env (car o))
        (cadr o)
      )
    )))))

(define (eratostene t0 max0)
  ;toto
  (let ([n 0])
  (let ([f 2])
  (let ([g (- max0 1)])
  (letrec ([d (lambda (i n) 
                (if (<= i g)
                (let ([n (if (eq? (vector-ref t0 i) i)
                         (let ([n (+ n 1)])
                         (block
                           (if (> (quotient max0 i) i)
                           (let ([j (* i i)])
                           (letrec ([e (lambda (j) 
                                         (if (and (< j max0) (> j 0))
                                         (block
                                           (vector-set! t0 j 0)
                                           (let ([j (+ j i)])
                                           (e j))
                                           )
                                         '()))])
                           (e j)))
                           '())
                         n
                         ))
                n)])
                (d (+ i 1) n))
                n))])
  (d f n)))))
)
(define main
  (let ([maximumprimes 1000001])
  (let ([era (array_init_withenv maximumprimes (lambda (j) 
                                                 (lambda (_) (let ([h j])
                                                             (list '() h)))) '())])
  (let ([nprimes (eratostene era maximumprimes)])
  (let ([primes (array_init_withenv nprimes (lambda (o) 
                                              (lambda (_) (let ([m 0])
                                                          (list '() m)))) '())])
  (let ([l 0])
  (let ([w 2])
  (let ([x (- maximumprimes 1)])
  (letrec ([v (lambda (k l) 
                (if (<= k x)
                (let ([l (if (eq? (vector-ref era k) k)
                         (block
                           (vector-set! primes l k)
                           (let ([l (+ l 1)])
                           l)
                           )
                         l)])
                (v (+ k 1) l))
                (block
                  (map display (list l " == " nprimes "\n"))
                  (let ([sum (array_init_withenv nprimes (lambda (i_) 
                                                           (lambda (_) 
                                                           (let ([p (vector-ref primes i_)])
                                                           (list '() p)))) '())])
                  (let ([maxl 0])
                  (let ([process #t])
                  (let ([stop (- maximumprimes 1)])
                  (let ([len 1])
                  (let ([resp 1])
                  (letrec ([q (lambda (len maxl process resp stop) 
                                (if process
                                (let ([process #f])
                                (let ([s 0])
                                (let ([u stop])
                                (letrec ([r (lambda (i maxl process resp stop) 
                                              (if (<= i u)
                                              ((lambda (internal_env) (apply (lambda
                                               (maxl process resp stop) 
                                              (r (+ i 1) maxl process resp stop)) internal_env)) 
                                              (if (< (+ i len) nprimes)
                                              (block
                                                (vector-set! sum i (+ (vector-ref sum i) (vector-ref primes (+ i len))))
                                                ((lambda (internal_env) (apply (lambda
                                                 (maxl process resp stop) 
                                                (list maxl process resp stop)) internal_env)) 
                                                (if (> maximumprimes (vector-ref sum i))
                                                (let ([process #t])
                                                ((lambda (internal_env) (apply (lambda
                                                 (maxl resp) 
                                                (list maxl process resp stop)) internal_env)) 
                                                (if (eq? (vector-ref era (vector-ref sum i)) (vector-ref sum i))
                                                (let ([maxl len])
                                                (let ([resp (vector-ref sum i)])
                                                (list maxl resp)))
                                                (list maxl resp))))
                                                (let ([stop (min stop i)])
                                                (list maxl process resp stop))))
                                                )
                                              (list maxl process resp stop)))
                                              (let ([len (+ len 1)])
                                              (q len maxl process resp stop))))])
                                (r s maxl process resp stop)))))
                                (block
                                  (map display (list resp "\n" maxl "\n"))
                                  )))])
                  (q len maxl process resp stop))))))))
    )))])
  (v w l)))))))))
)

