
void sort_(integer array tab, integer len){
	for i := 0 to len -1
	{
	   for j := i + 1 to len - 1
	   {
		if (tab[i] > tab[j]){
		   integer tmp := tab[i];
		   tab[i] := tab[j];
		   tab[j] := tmp;
		}
	   }
	}
}

prog{
	integer len := 2;
	read<integer>(len);
	stdin_sep();
	integer array tab[len];
	for i := 0 to len - 1
	{
		integer tmp := 0;
		read<integer>(tmp);
		stdin_sep();
		tab[i] := tmp;
	}
	sort_(tab, len);
	print<integer array>(tab);
}