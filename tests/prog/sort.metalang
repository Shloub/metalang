
void sort_(integer array tab, integer len){
	for i := 0 to len -1
	{
	   for j := i + 1 to len - 1
	   {
		if (tab[i] > tab[j]){
		   var integer tmp := tab[i];
		   tab[i] := tab[j];
		   tab[j] := tmp;
		}
	   }
	}
}

prog{
	var integer len := 2;
	read<integer>(len);
	stdin_sep();
	dim integer array tab[len];
	for i := 0 to len - 1
	{
		var integer tmp := 0;
		read<integer>(tmp);
		stdin_sep();
		tab[i] := tmp;
	}
	sort_(tab, len);
	print<integer array>(tab);
}