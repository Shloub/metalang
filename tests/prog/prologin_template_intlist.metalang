
def int programme_candidat(array<int> tableau, int taille)
  def out = 0
  for i = 0 to taille - 1 do
    out += tableau[i]
  end
  return out
end

main
  def taille = read_int()
  def tableau = read_int_line(taille)
  print programme_candidat(tableau, taille) print "\n"
end
