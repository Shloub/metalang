def int nth(array<char> tab, char tofind, int len)
	def int out = 0
	for i = 0 to len - 1 do
		if tab[i] == tofind then
		   out = out + 1
		end
	end
	return out
end

main
	def int len = 0
	read int len skip
	def char tofind = '\000'
	read char tofind skip
	def array<char> tab[len] with i do
		def char tmp = '\000'
		read char tmp
		return tmp
	end
	def int result = nth(tab, tofind, len)
	print int result
end
