function integer double(integer %a){
	 if (%a == 0){
	    return %a * 2;
	 }else{
		return %a * 2;
	 }
}

function void foo(integer %a, float %b){
	 %a := double(%a);
	 pinteuh(%a + double(%b));
}

prog{
	var integer %a := 2;
	var integer %b := 3;
	var integer %i := 0;
	dim integer array %tab[%a];
	for %i := 0 to 5 step 1
	{
		%a := %a + 1;
	}
	foo(%a + %b);
	read<integer>(%a);
	print<integer>(%b + %a);
	print<integer array>(%tab);
}