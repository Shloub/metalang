function integer double_(integer %a){
	 if (%a == 0){
	    return %a * 2;
	 }else{
		return %a * 2 + 1;
	 }
}

function void foo(integer %a, integer %b){
	 print<integer>(%a + double_(%b));
}

function void testStr(){
	 var string %str := " \" foo";
	 var bool %b := true;
}

prog{
	testStr();
	var integer %a := 2;
	var integer %b := 3;
	read<integer>(%a);
	dim integer array %tab[%a];
	for %i := 0 to %a step 1
	{
		%tab[%i] := %i * (%i + 1) / 2;
		%b := %b + %tab[%i];
	}
	foo(%a, %b);
	print<integer>(%b + %a);
	print<integer array>(%tab);
}