integer double_(integer a){
	 if (a == 0){
	    return a * 2;
	 }else{
		return a * 2 + 1;
	 }
}

void foo(integer a, integer b){
	 print<integer>(a + double_(b));
}

void testStr(){
	 string str := " \" foo";
	 bool b := true && (false || (!true) == false ) && 1 mod 2 == 0 ;
	 #read<string>(str);
	 integer d := 0;
	 read<integer>(d);
	 stdin_sep();
	 char array tab[d];
	 for i := 0 to d-1
	 {
		char c := ' ';
		read<char>(c);
		tab[i] := c;
	 }
	 stdin_sep();
}

prog{
	testStr();
	integer a := 2;
	integer b := 3;
	read<integer>(a);
	integer array tab[a];
	for i := 0 to a-1
	{
		tab[i] := i * (i + 1) / 2;
		b := b + tab[i];
	}
	foo(a, b);
	print<integer>(b + a);
	print<integer array>(tab);
}