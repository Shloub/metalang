Langage & Compilateur :
améliorer les erreurs du typer
gérer les entrées d'entiers négatifs (en C#, php, python)
php stdin : ne pas forcer à faire ctrl+d
number of args checker

templates
types abstract (pour ouvrir un fichier par exemple)

return checker
gérer les floats (parser, +., *., /., + printers)
lambda ast + typeAst + syntax + lambdalifting
  eg : fun int x, int y = x + y end
warnings pour les choses laides dans les différents langages
virer les warnings
AST : foreach like : ça serait plus beau dans pas mal de
  langages pour tout les parcours de tableaux
break et continue (attention au caml)
commentaires indentation
chercher a faire un sort correct

autres printers :
  javascript
  pascal
  f#
  vb.net
  lisp
  perl
  bash
  ada
  html (code printer)