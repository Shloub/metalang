accélérer les tests :
  euler07

Langage & Compilateur :
  C if indentation
  smalltalk macros
  forth macros & return intelligent
  structures as couples pour les langages qui le supportent
  tableau constant lors d'une initialisation
  types abstract & types alias (pour ouvrir un fichier ou pour les bigints par exemple)
  type option
  une lib standard avec des listes et des arbres
  un pseudo systeme objet

outils :
  un meilleur mode vim
  un mode eclipse ou code blocks
  un truc de completion pour bash
  une man page

autres printers :
  erlang
  rhino
  clojure
  rust

portage windows

tests :
  prologin_template_dico
