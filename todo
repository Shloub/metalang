clarifier la boucle for : syntaxe et sémentique
mieux gérer else if ou if sans else
type ast
type checker
names checker
autres printers
virer les warnings

gestion des chaines
mapping de fonctions (de chaines par exemple): multiBSL

script bash de test

AST : foreach like : ça serait plus beau dans pas mal de langages pour tout les parcours de tableaux
alloc array avec instructions d'initialisation
